server:
  port: 30005
spring:
  main:
    allow-circular-references: true
  application:
    name: orderService # 服务名称

springdoc:
  group-configs:
    - group: 'default'
      display-name: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.jh.education.order.controller
